import { JsonTablePipe } from './json-table.pipe';

describe('JsonTablePipe', () => {
  it('create an instance', () => {
    const pipe = new JsonTablePipe();
    expect(pipe).toBeTruthy();
  });
});
